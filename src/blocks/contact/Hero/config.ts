import type { Block } from 'payload';

const ContactHero: Block = {
  slug: 'contactHero',
  interfaceName: 'ContactHeroBlock',
  labels: {
    singular: 'Contact Hero Block',
    plural: 'Contact Hero Blocks',
  },
  fields: [
   
  ]
};

export default ContactHero; 